const brand = 'ferrari';
const model = 296;
const productYear = 1996;
let name = 'Mario Rossi'
let used = false;

if (used) {
    console.log (brand)
} else {
    console.log ("l/auto usata è" + brand);
}

